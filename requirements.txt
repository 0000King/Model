# Core dependencies
pandas
langchain
langchain-community
chromadb
openai
tiktoken
langchain_google_genai
openpyxl

# Optional but common for env setup
python-dotenv

# FastAPI or Flask (choose one if you expose your model as an API)
fastapi
uvicorn[standard]

# Needed for Render deployment
gunicorn
